"use strict";

/* This is to prevent code duplicates since language is used in both Typescript & Javascript files */
const language = {
  default: "en",
  supported: ["en"],
};

module.exports = language;
